// src/screens/WorkoutsScreen.js
import { View, Text } from "react-native";

export default function ActiveSessionScreen() {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: "#121212",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text style={{ color: "#fff" }}>ActiveSession</Text>
    </View>
  );
}
